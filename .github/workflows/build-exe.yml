name: 💻 Build EXE on Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧬 Checkout Code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: 🔨 Build EXE with PyInstaller
        run: |
          pyinstaller --noconfirm --clean --onefile Madmen_AutoLogin_v4.py

      - name: 📤 Upload Compiled EXE
        uses: actions/upload-artifact@v3.1.3  # ✅ Fully qualified version here
        with:
          name: Madmen_Login_Hack_EXE         # 🔖 Artifact name shown in GitHub UI
          path: dist/Madmen_AutoLogin_v4.exe  # 📍 Path to the compiled .exe file
